export enum UiColor {
  Primary = 'primary',
  InProgress = 'in-progress',
  Warning = 'warning',
  Success = 'success',
  Error = 'error',
  Default = 'default',
}

export enum UiSize {
  Sm = "sm",
  Md = "md",
  Lg = "lg",
}
